# 小智智能语音设备业务逻辑流程图

## 1. 设备启动与激活流程

```mermaid
flowchart TD
    A[设备上电启动] --> B[硬件初始化]
    B --> C{LCD显示启动界面}
    C --> D[初始化LED/按钮]
    D --> E[初始化NVS存储]
    E --> F[初始化WiFi]
    F --> G{需要配网?}
    G -->|是| H[显示配网二维码]
    G -->|否| I[初始化音频Codec]
    H --> I
    I --> J[OTA激活检查]
    J --> K{设备已激活?}
    K -->|否| L[显示激活码]
    L --> M[等待用户扫码激活]
    M --> J
    K -->|是| N[获取WebSocket连接信息]
    N --> O[初始化协议模块]
    O --> P[初始化音频处理器]
    P --> Q[注册事件回调]
    Q --> R[进入空闲状态<br/>等待唤醒]
```

## 2. 语音交互完整流程

```mermaid
flowchart TD
    A[空闲状态<br/>APP_STATE_IDLE] --> B{检测到唤醒词<br/>"你好小智"}
    B --> C[连接中状态<br/>APP_STATE_CONNECTING]
    C --> D[建立WebSocket连接]
    D --> E{连接成功?}
    E -->|是| F[发送HELLO消息]
    F --> G[唤醒状态<br/>APP_STATE_WAKEUP]
    E -->|否| A
    G --> H{检测到语音?}
    H -->|是| I[监听状态<br/>APP_STATE_LISTENING]
    I --> J[开始录音并上传]
    J --> K{检测到静音?}
    K -->|是| L[停止录音]
    L --> G
    H -->|否| M{超时5秒?}
    M -->|是| N[断开连接]
    N --> A
    M -->|否| G

    subgraph 服务器处理
        J --> O[云端STT服务]
        O --> P[文本识别结果]
        P --> Q[LLM情感分析]
        Q --> R[生成回复]
        R --> S[TTS语音合成]
    end

    S --> T[收到TTS开始事件]
    T --> U[讲话状态<br/>APP_STATE_SPEAKING]
    U --> V[播放TTS音频]
    V --> W{音频播放完成?}
    W -->|是| X[收到TTS结束事件]
    X --> G
    W -->|否| Y{检测到打断唤醒?}
    Y -->|是| Z[中止播放<br/>重新唤醒]
    Z --> G
    Y -->|否| V
```

## 3. 状态机详细转移图

```mermaid
stateDiagram-v2
    [*] --> STARTING: 设备启动
    STARTING --> ACTIVATING: 需要激活
    ACTIVATING --> STARTING: 激活完成
    STARTING --> IDLE: 初始化完成

    IDLE --> CONNECTING: 唤醒词检测
    CONNECTING --> WAKEUP: 连接成功+HELLO
    CONNECTING --> IDLE: 连接失败

    WAKEUP --> LISTENING: 检测到语音
    LISTENING --> WAKEUP: 静音检测

    WAKEUP --> SPEAKING: 收到TTS开始
    SPEAKING --> WAKEUP: TTS结束

    WAKEUP --> IDLE: 超时断开
    SPEAKING --> WAKEUP: 打断唤醒

    note right of IDLE
        等待唤醒状态
        VAD关闭
        可被唤醒词触发
    end note

    note right of WAKEUP
        已唤醒状态
        VAD开启
        等待语音输入
        5秒超时计时
    end note

    note right of LISTENING
        录音状态
        实时上传音频
        等待静音检测
    end note

    note right of SPEAKING
        播放状态
        VAD关闭
        播放TTS音频
        可被唤醒词打断
    end note
```

## 4. 事件驱动架构图

```mermaid
flowchart TD
    subgraph 硬件层
        A1[麦克风] --> A2[音频Codec]
        B1[按钮] --> B2[GPIO中断]
        C1[WiFi模块] --> C2[网络事件]
    end

    subgraph BSP层
        A2 --> D1[音频驱动]
        B2 --> D2[按钮驱动]
        C2 --> D3[WiFi驱动]
        D1 --> D4[BSP事件总线]
        D2 --> D4
        D3 --> D4
    end

    subgraph 业务层
        D4 --> E1[音频处理器]
        D4 --> E2[协议模块]
        E1 --> F1[唤醒词检测]
        E1 --> F2[VAD检测]
        E1 --> F3[音频编码]
        E2 --> F4[WebSocket连接]
        E2 --> F5[消息解析]

        F1 --> G1[WAKEUP事件]
        F2 --> G2[SPEECH/SILENCE事件]
        F4 --> G3[CONNECTED/DISCONNECTED事件]
        F5 --> G4[STT/LLM/TTS事件]

        G1 --> H[应用状态机]
        G2 --> H
        G3 --> H
        G4 --> H
    end

    subgraph 响应层
        H --> I1[状态更新]
        H --> I2[音频播放]
        H --> I3[消息发送]
        H --> I4[UI更新]

        I1 --> J1[LED状态]
        I2 --> J2[扬声器]
        I3 --> J3[云端服务器]
        I4 --> J4[LCD显示]
    end
```

## 5. 数据流示意图

```mermaid
flowchart LR
    subgraph 输入数据流
        A[用户语音] --> B[麦克风]
        B --> C[PCM音频<br/>16kHz/16bit]
        C --> D[音频处理器]
        D --> E[Opus编码]
        E --> F[WebSocket上传]
    end

    subgraph 处理数据流
        F --> G[云端服务器]
        G --> H[STT服务]
        H --> I[文本结果]
        I --> J[LLM服务]
        J --> K[情感+回复]
        K --> L[TTS服务]
        L --> M[Opus音频]
    end

    subgraph 输出数据流
        M --> N[WebSocket下发]
        N --> O[音频处理器]
        O --> P[Opus解码]
        P --> Q[PCM音频]
        Q --> R[扬声器]
        R --> S[用户听到回复]
    end

    subgraph 控制数据流
        T[状态信息] --> U[UI显示]
        V[WiFi信号] --> U
        W[电池电量] --> U
        X[设备状态] --> U
    end
```

## 6. 异常处理流程图

```mermaid
flowchart TD
    A[异常发生] --> B{异常类型}

    B --> C[网络断开]
    B --> D[硬件故障]
    B --> E[音频异常]
    B --> F[内存不足]

    C --> G[触发DISCONNECTED事件]
    G --> H[应用状态: 当前 → IDLE]
    H --> I[关闭VAD检测]
    I --> J[等待重新唤醒]

    D --> K[BSP状态检查]
    K --> L{检查通过?}
    L -->|是| M[记录日志<br/>继续运行]
    L -->|否| N[进入安全模式]
    N --> O[显示错误信息]
    O --> P[等待重启]

    E --> Q[音频处理器重置]
    Q --> R[重新初始化Codec]
    R --> S[恢复音频流程]

    F --> T[堆内存监控]
    T --> U[释放非关键资源]
    U --> V[尝试恢复]
    V --> W{恢复成功?}
    W -->|是| X[继续运行]
    W -->|否| Y[安全重启]
```

## 7. 模块依赖关系图

```mermaid
graph TB
    A[main.c] --> B[application.c]
    B --> C[audio_processor.c]
    B --> D[protocol.c]
    B --> E[ui.c]
    B --> F[ota.c]

    C --> G[audio_sr.c]
    C --> H[audio_encoder.c]
    C --> I[audio_decoder.c]

    D --> J[esp_websocket_client]
    D --> F

    E --> K[LVGL库]

    F --> L[HTTP客户端]

    B --> M[bsp_board.c]
    M --> N[bsp_board_button.c]
    M --> O[bsp_board_codec.c]
    M --> P[bsp_board_lcd.c]
    M --> Q[bsp_board_led.c]
    M --> R[bsp_board_wifi.c]

    N --> S[iot_button库]
    O --> T[esp_codec_dev库]
    P --> U[esp_lcd库]
    Q --> V[led_indicator库]
    R --> W[WiFi配网库]

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style B fill:#ccf,stroke:#333,stroke-width:2px
    style C fill:#cfc,stroke:#333,stroke-width:1px
    style D fill:#cfc,stroke:#333,stroke-width:1px
    style E fill:#cfc,stroke:#333,stroke-width:1px
    style F fill:#cfc,stroke:#333,stroke-width:1px
    style M fill:#ffc,stroke:#333,stroke-width:2px
```

## 关键时序说明

### 典型交互时序
1. **唤醒阶段**：<200ms响应时间
2. **连接阶段**：1-3秒网络连接时间
3. **录音阶段**：用户说话时长 + 静音检测
4. **处理阶段**：云端处理时间（STT+LLM+TTS）
5. **播放阶段**：TTS音频播放时间

### 超时控制
- **唤醒超时**：5秒无语音输入 → 断开连接
- **网络超时**：连接建立超时 → 重试机制
- **录音超时**：最长录音时间限制

### 实时性要求
- **音频采集**：实时无延迟
- **唤醒检测**：<200ms延迟
- **VAD检测**：实时检测
- **UI更新**：<100ms响应

这些流程图清晰地展示了小智智能语音设备的完整业务逻辑，从设备启动到语音交互的各个环节，以及异常处理和模块关系。